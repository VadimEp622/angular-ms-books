

export async function queryBooksByGenres(genres) {
    console.log('book.service - queryBooksBy -> genres', genres)
    // TODO: fetch books by genres from open-library API and return array of objects, where each object is a genre and its books,
    // e.g. {genre: 'Fantasy', books: [book1, book2, ...]} ,
    // and each books only has the keys we need.
    // 
    return []
}